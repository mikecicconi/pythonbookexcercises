#Without running the following code, what do you think it will do?

def foo(bar, qux): # defined foo with 2 parameters
    print(bar)      # call to print
    print(qux)      # call to print

foo('Hello')        # passing a string 'Hello', assigned to bar, needs two arguments to match the parameters from foo